{
  "name": "mix-skill-people",
  "version": "1.0.0",
  "private": true,
  "description": "AI 人格合成工具",
  "scripts": {
    "setup": "npx playwright install chromium",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
